name: flutter_trigger_input_example

description: Example project for flutter_trigger_input
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  faker: ^2.2.0
  flutter_trigger_input:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
